codecov:
  branch: master

coverage:
  precision: 2
  round: nearest
  range: "60...100"
  ignore:
    - Tests/*
    - Example/*

  status:
    project:
      default:
        target: auto
        branches:
          - master
          - develop

comment:
  layout: "header, diff, changes, sunburst, uncovered, tree"
  behavior: default
  branches:
    - master
    - develop
